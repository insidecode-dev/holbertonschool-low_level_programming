#include "main.h"
/**
 * add - adds two integers and returns result
 * @a: first integer
 * @b: second integer
 * Description: adds two integers and returns result
 * Return: Integer
 **/
int add(int a, int b)
{
  return (a+b);
}
