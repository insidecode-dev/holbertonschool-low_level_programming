#include "main.h"
#include <ctype.h>
/**
 * _isalpha - computes the absolute value of an integer
 * @c: character
 * Description: This function computes the absolute value of an integer
 * Return: Integer
 **/
int _abs(int c)
{
return (abs(c));
}
