#include "main.h"
#include <ctype.h>
/**
 * print_alphabet - prints alphabet in lowercase
 * Description: This function prints alphabet in a lowercase
 **/
int _islower(int c)
{
return (islower(c));
}
