#include<stdio.h>
#include <string.h>
/**
 * _strlen - returns length of string
 * @s: pointer to string
 * Description: This function returns length of string
 * Return: Integer
 **/
int _strlen(char *s)
	{
		return (strlen(s));
	}
