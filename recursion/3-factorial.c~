#include "main.h"
/**
 * factorial - calculates factorial of input parameter
 * @n: number
 * Return: integer
 */
int factorial(int n)
{
	if (n < -1)
		return -1;
	if (n == 1)
		return 1;
	return (n * factorial(n - 1));
}
